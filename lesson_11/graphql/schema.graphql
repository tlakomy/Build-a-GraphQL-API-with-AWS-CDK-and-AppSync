type Book {
  id: ID!
  name: String!
  completed: Boolean
  rating: Int
  reviews: [String]
}

input BookInput {
  id: ID!
  name: String!
}

type Query {
  listBooks: [Book]
  getBookById(bookId: ID!): Book
}

type Mutation {
  createBook(book: BookInput!): Book
}
